// Tailwind v4 configures through this PostCSS plugin; there is deliberately no
// tailwind.config.js — v4 is CSS-first (see app/globals.css).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
